from enum import StrEnum

class AcctType(StrEnum):
    """
    Represents the gender types that can be stored in the gender category.
    """
    blue, red, yellow, personal, unknown = ["blue", "red", "yellow", "personal", "unknown"]
